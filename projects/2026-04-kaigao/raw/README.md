# raw/（项目专属资料）

> 本目录存放 kaigao 项目相关的**原始资料**（会议记录、需求文档、产品截图、对话截图、第三方参考资料等）。

与顶层 `raw/` 的区别：
- 顶层 `raw/` = 通用资料（会被多个项目 / 概念引用）
- 本目录 = 仅本项目消费的素材

原则（与顶层 raw 一致）：**原文不修改**。提炼放到 `notes/` 或 `deliverables/`。
